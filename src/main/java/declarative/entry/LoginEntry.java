package declarative.entry;

import declarative.OpenApi;
import declarative.requests.LoginReq;
import declarative.responses.LoginRes;

public class LoginEntry implements OpenApi {
    @Override
    public String call(String request) {
        //LoginRes loginRes = new LoginRes();
        //loginRes.setToken("xxxxxxxx");
        return "xxxxx";
    }
}
